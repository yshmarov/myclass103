= simple_form_for([:admin, @expence]) do |f|
  = f.error_notification

  .form-inputs
    = f.input :amount
    = f.input :description
    = f.association :user

  .form-actions
    = f.button :submit
